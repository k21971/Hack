# .cirrus.yml — FreeBSD build sanity for restoHack

task:
  name: FreeBSD build (curses sanity)
  freebsd_instance:
    image_family: freebsd-14-2

  env:
    MAKEFLAGS: -j$(sysctl -n hw.ncpu)
    CIRRUS_CLONE_DEPTH: "1"

  install_script:
    - sudo pkg update -f
    - sudo pkg install -y gmake cmake ninja pkgconf ncurses

  build_script:
    - cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
    - cmake --build build -j

  verify_script:
    - file build/hack || true
    - printf '#include <curses.h>\nint main(){initscr();endwin();return 0;}\n' > /tmp/t.c
    - cc /tmp/t.c -lcurses -o /tmp/t && /tmp/t || true
